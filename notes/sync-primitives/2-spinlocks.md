Spinlock is the most primitive synchronization primitive. It uses tactic like "wait in a loop until it is allowed to access critical section block execution in my thread".

Implementation in `C`:
```c
```
